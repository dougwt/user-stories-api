{
  "name": "user-stories-react-api",
  "version": "0.1.0",
  "private": true,
  "babel": {
    "presets": [
      "latest"
    ]
  },
  "scripts": {
    "start": "nf start -p 3000",
    "server": "API_PORT=3001 NODE_ENV=dev ./node_modules/.bin/nodemon server/index.js --exec babel-node",
    "test": "NODE_ENV=test mocha --compilers js:babel-core/register --recursive ./server/test",
    "test:watch": "NODE_ENV=test ./node_modules/.bin/nodemon --exec 'mocha --compilers js:babel-core/register --recursive ./server/test'"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.21.0",
    "babel-preset-latest": "^6.16.0",
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "foreman": "^2.0.0",
    "mocha": "*",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "mongoose": "^4.7.6",
    "morgan": "^1.7.0"
  }
}
